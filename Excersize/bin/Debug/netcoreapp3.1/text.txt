# Rules
#
# 1.Strongly-typed, oop language: Compiled to Common Intermidiary Language(CIL)
#   a. int(alias for BCL type System.Int32)
# 3. Syntax:
#    a. Namespace: namespace
#    b. Function: method
#    c. Class: class
#    d.Types: (int|bool|string) 
#    e. Variable: local [Type] [Identifier]
#    n. Keywords: (Namespace|Function|Class)
#    x. Char Literals: '\\?.'
#    y. String Literals: \".*?\"
#    z. Identifier: [A-Za-z_]\w*
# 5. Case-Sens
namespace MyNameSpace
{
    class Program
    {
	
        static method int GetNumber()
        {
            Print("Pick rock(1), paper(2), scissor(3)");
            return int.Parse(Read());
        }
        static method entrypoint Main()
        {
            local bool isPlaying = true;
            local int x[5];
            while(isPlaying)
            {
                local int Answer = GetNumber();
                local int Computer = 2;
                local int a = -1;
		local bool l = a >= 1;
                if(Computer == 1 && a == -1)
                {
                   Print("Tie");
                }
                else if(Computer == 2 || a == 1)
                {
	       	        Print("Win");
                }
                else
                {
                   Print("Loss");
                }
                local char Again = Console.WriteLine("Want to play again(y/n)?");
                if(Again == 'n')
                {
                    isPlaying = false;
                }
            }
	        Pause();
        }
    }
}